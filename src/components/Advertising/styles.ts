import styled from "styled-components";
import { Colors, baseBox } from "theme";

export const Container = styled.div`
${baseBox}
  background-color: ${Colors.midnightNavy};
  width: 300px;
  height: 820px;
  border: 1px solid ${Colors.lightBlueGrayTransparent};
`
